# Copy to .env.local and fill in. NEVER prefix with NEXT_PUBLIC_ (hard rule #7).
# Read only inside app/api/reconcile/route.ts — never in client code.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
ANTHROPIC_MODEL=claude-opus-4-8
